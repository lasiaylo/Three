using System;

namespace Util.Utils {
	public static class MathVal {
		// This is too clever, but it's staying in.
		public static readonly double PII = Math.PI * 2;

		public static readonly float PIIF = (float) PII;
	}
}